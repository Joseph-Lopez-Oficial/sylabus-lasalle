name: Release

# Triggers on every push to main. semantic-release will determine
# if a new version is needed based on conventional commit messages.
on:
    push:
        branches:
            - main

jobs:
    release:
        name: Semantic Release
        runs-on: ubuntu-latest

        # Required permissions for semantic-release to:
        # - Push commits back to the repo (package.json, composer.json, CHANGELOG.md)
        # - Create GitHub releases and tags
        permissions:
            contents: write
            issues: write
            pull-requests: write

        steps:
            - name: Checkout repository
              uses: actions/checkout@v5
              with:
                  # Fetch full history so semantic-release can read all commits
                  fetch-depth: 0
                  # RELEASE_TOKEN is a personal access token allowed to bypass
                  # the branch protection on main. GITHUB_TOKEN cannot: pushes
                  # made with it are rejected by the "require pull request" rule.
                  token: ${{ secrets.RELEASE_TOKEN }}
                  persist-credentials: true

            - name: Setup Node.js
              uses: actions/setup-node@v5
              with:
                  node-version: 'lts/*'
                  cache: 'npm'

            - name: Setup PHP
              uses: shivammathur/setup-php@v2
              with:
                  php-version: '8.4'
                  tools: composer:v2

            - name: Install PHP Dependencies
              run: composer install --no-interaction --prefer-dist --optimize-autoloader

            - name: Install Node dependencies
              run: npm ci

            - name: Run semantic-release
              env:
                  # The same bypass token is needed here: @semantic-release/git
                  # pushes the version bump commit back to the protected branch.
                  GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}
                  HUSKY: 0
              run: npx semantic-release
